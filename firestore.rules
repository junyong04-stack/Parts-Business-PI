rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Requires a signed-in user (anonymous sign-in counts) so the public GitHub
    // Pages URL alone isn't enough to read or edit team data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
